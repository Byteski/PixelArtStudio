[project]
name = "pixel-art-studio"
version = "0.1.0"
description = "A compact desktop image-to-pixel-art converter"
requires-python = ">=3.10"
dependencies = [
  "Pillow>=11.0,<13",
  "PySide6>=6.8,<7",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
